# NATS Server Configuration for Testing

# Enable JetStream
jetstream {
  # Store directory
  store_dir: "/data"
  
  # Limit resources for testing
  max_memory_store: 100MB
  max_file_store: 1GB
}

# Server limits
max_payload: 1MB

# Logging
debug: false
trace: false